
&НаКлиенте
Процедура ВыбратьДокумент(Команда)
	Если ЗначениеЗаполнено(ДокументИсточникЗаполнения) Тогда
		ПараметрыЗакрытия = Новый Структура();
		ПараметрыЗакрытия.Вставить("ДокументИсточникЗаполнения", ДокументИсточникЗаполнения);
		ЭтаФорма.Закрыть(ПараметрыЗакрытия);
	Иначе
		ОбщегоНазначенияКлиентСерверУ.СообщитьПользователю("Не выбран исходный документ.");
	КонецЕсли; 
КонецПроцедуры
