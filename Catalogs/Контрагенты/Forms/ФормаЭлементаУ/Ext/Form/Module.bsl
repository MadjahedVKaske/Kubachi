
&НаСервере
&ИзменениеИКонтроль("ИННПриИзменении")
Процедура B1_ИННПриИзменении()
	
	#Вставка
	Если НЕ ЗначениеЗаполнено(Объект.СтранаРегистрации) ИЛИ Объект.СтранаРегистрации.Код = "643" Тогда
	#КонецВставки
	ОшибкаИНН = ОбщегоНазначения.ИННсоответствуетТребованиям(Объект.ИНН, Объект.ЮрФизЛицо);
	#Вставка
	Иначе
	ОшибкаИНН = "";
	КонецЕсли;
	#КонецВставки
	Элементы.ОшибкаПроверкиИНН.Заголовок = ОшибкаИНН;
	Элементы.ОшибкаПроверкиИНН1.Заголовок = ОшибкаИНН;

КонецПроцедуры
